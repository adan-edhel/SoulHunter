using UnityEngine;

public class Interactable : MonoBehaviour // Mort
{
    // Whether the entity is interactable
    public bool isActivatable = true;

    // Whether the entity can be activated
    // more than once
    public bool isRepeatable;
}
